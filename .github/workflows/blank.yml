# This is a basic workflow to help you get started with Actions

name: Build

on: workflow_dispatch

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Environment
        run: sudo apt install debootstrap squashfs-tools grub2-common xorriso mtools

      - name: Build Process
        run: |
          sudo chmod +x build.sh
          sudo chmod +x chroot.sh
          sudo chmod +x make-live.sh
          sudo chmod +x make-iso.sh

          sudo ./build.sh
          sudo ./make-live.sh
          sudo ./make-iso.sh
          ls output -lh         
